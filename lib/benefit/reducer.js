const actionType = require('./actions');

const initialState = { benefits: {} };

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

module.exports = {
  reducer
};
